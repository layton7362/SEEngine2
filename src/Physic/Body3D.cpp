
#include <Physic/Body3D.hpp>

Body3D::Body3D()
{
}

Body3D::~Body3D()
{
}

void Body3D::addShape(Shape3D &shape)
{
    // auto it = std::find(shapes.begin(), children.end(), shape);
    // if (it != children.end())
    // {
    //     children.erase(it);
    //     child->parent = nullptr;
    // }
}
